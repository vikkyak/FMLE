% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R, R/cv_old.R
\name{fmle_cv_mt_parallel}
\alias{fmle_cv_mt_parallel}
\title{Multi-task cross-validation for FMLE}
\usage{
fmle_cv_mt_parallel(
  X,
  Y,
  Z,
  R_grid = c(4, 6, 8),
  m_grid = c(1.6, 1.8, 2),
  lambda_grid = c(0, 0.001, 0.01, 0.03),
  folds = 5,
  groups = NULL,
  ridge = 1e-06,
  standardize = TRUE,
  seed = 1,
  exec = c("sequential", "future"),
  verbose = TRUE,
  task_weights = NULL
)

fmle_cv_mt_parallel(
  X,
  Y,
  Z,
  R_grid = c(4, 6, 8),
  m_grid = c(1.6, 1.8, 2),
  lambda_grid = c(0, 0.001, 0.01, 0.03),
  folds = 5,
  groups = NULL,
  ridge = 1e-06,
  standardize = TRUE,
  seed = 1,
  exec = c("sequential", "future"),
  verbose = TRUE,
  task_weights = NULL
)
}
\arguments{
\item{X}{matrix, n x p features.}

\item{Y}{matrix, n x T responses (e.g., multiple proteins).}

\item{Z}{matrix, n x d gating features.}

\item{R_grid}{grid of R values.}

\item{m_grid}{grid of m values.}

\item{lambda_grid}{grid of lambda_l1 values.}

\item{folds}{number of folds.}

\item{groups}{optional factor of length n for grouped CV.}

\item{ridge}{ridge penalty.}

\item{standardize}{logical.}

\item{seed}{random seed.}

\item{exec}{"sequential" or "future".}

\item{verbose}{logical.}

\item{task_weights}{optional length-T vector of task weights; NULL = equal.}
}
\value{
A list with components:
\describe{
\item{best}{List with best (R, m, lambda, mse).}
\item{table}{Data frame of configs and their weighted mean MSE.}
}

A list with components:
\describe{
\item{best}{List with best (R, m, lambda, mse).}
\item{table}{Data frame of configs and their weighted mean MSE.}
}
}
\description{
Performs K-fold multi-task cross-validation over grids of R, m, and
lambda_l1 using \code{fmle_train_mt} / \code{fmle_predict_mt}, with
optional task weighting and grouped folds.

Performs K-fold multi-task cross-validation over grids of R, m, and
lambda_l1 using \code{fmle_train_mt} / \code{fmle_predict_mt}, with
optional task weighting and grouped folds.
}

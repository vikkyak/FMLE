% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{fmle_cv_parallel}
\alias{fmle_cv_parallel}
\title{Cross-validation for single-task FMLE}
\usage{
fmle_cv_parallel(
  X,
  y,
  Z,
  R_grid = c(4, 6, 8),
  m_grid = c(1.6, 1.8, 2),
  lambda_grid = c(0, 0.001, 0.01, 0.03),
  folds = 5,
  groups = NULL,
  ridge = 1e-06,
  standardize = TRUE,
  seed = 1,
  exec = c("sequential", "future"),
  verbose = TRUE,
  q = 0.995
)
}
\arguments{
\item{X}{matrix, n x p features.}

\item{y}{numeric response vector of length n.}

\item{Z}{matrix, n x d gating features (e.g., PCs).}

\item{R_grid}{grid of R values.}

\item{m_grid}{grid of m values.}

\item{lambda_grid}{grid of lambda_l1 values.}

\item{folds}{number of CV folds.}

\item{groups}{optional factor of length n for grouped CV; NULL gives random folds.}

\item{ridge}{small ridge penalty for stability}

\item{standardize}{logical; standardize X and Z}

\item{seed}{random seed}

\item{exec}{character, "sequential" or "future" (for future_lapply).}

\item{verbose}{logical; print progress messages.}

\item{q}{Quantile used to cap the response before log1p/scale preprocessing in cross-validation.}
}
\value{
A list with components:
\describe{
\item{best}{List with the best (R, m, lambda, mse).}
\item{table}{Data frame of all configs and their CV MSE.}
}
}
\description{
Performs K-fold cross-validation over grids of R (number of experts),
m (fuzzifier), and lambda_l1 (L1 penalty), optionally using grouped
folds and future-based parallelization.
}
